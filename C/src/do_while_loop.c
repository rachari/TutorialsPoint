#include <stdio.h>

int main()
{
    /* local variable declaration */
    int a = 10;

    /* do loop execution */
    do
    {
        printf("Value of a: %d\n", a);
        a++;
    }
    while (a < 20);
    return 0;
}
