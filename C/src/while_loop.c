#include <stdio.h>

int main()
{
    /* local variable declaration */
    int a = 10;

    /* while loop execution */
    while ( a < 20 )
    {
        printf("Value of a: %d\n", a);
        a++;
    }
    return 0;
}
